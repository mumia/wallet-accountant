# Reports

Generated reports from `/edikt:sdlc:audit`, `/edikt:gov:score`, `/edikt:sdlc:drift`, `/edikt:sdlc:review`, etc.

Reports are dated and idempotent — re-running a command produces a new report alongside prior ones.
