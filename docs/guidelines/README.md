# Guidelines

Capture team conventions (Kotlin idioms, Spring boundaries, Axon patterns, multi-tenant rules) with: "add a guideline" or `/edikt:guideline:new`

Format: free — extended template at `.edikt/templates/guideline.md`. Every bullet under `## Rules` MUST use MUST/NEVER language.
