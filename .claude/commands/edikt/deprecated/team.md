---
name: edikt:team
description: "Deprecated — use /edikt:init"
effort: low
---
# ⚠️ This command has moved

`/edikt:team` is deprecated. Its functionality has been merged into `/edikt:init` and `/edikt:config`.

**Use instead:**
- `/edikt:init` — validates your local environment when the project is already set up
- `/edikt:config` — view and modify project configuration

This stub will be removed in v0.5.0.
