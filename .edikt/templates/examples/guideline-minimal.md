# {Topic Title} Guidelines

**Purpose:** {One sentence — what problem this guideline prevents.}

<!--
Minimal guideline template (edikt).

Guidelines are softer than Invariant Records but still enforceable.
Every rule in the Rules section must use MUST or NEVER language —
soft language ("should", "prefer", "try to") does not compile into
governance.

Writing guidance:
1. Every rule is a MUST or a NEVER. If it's not, it's a suggestion,
   not a guideline — omit it.
2. Rules should be specific enough to verify. "Error messages must
   be helpful" is too vague. "Error messages must include the failing
   input and the expected format" is verifiable.
3. Guidelines are cross-cutting within a topic (e.g. all testing, all
   logging). Rules that only apply to one file belong in that file.

See guideline-extended.md for a template with Examples and Rationale
sections. See the invariant writing guide for additional discipline
that also applies to guidelines.
-->

## Rules

- {Rule 1 using MUST or NEVER — specific and verifiable}
- {Rule 2 using MUST or NEVER — specific and verifiable}
- {Rule 3 using MUST or NEVER — specific and verifiable}

---

*Created by edikt:guideline — {date}*

<!-- Directives for edikt governance. Populated by /edikt:guideline:compile. -->
[edikt:directives:start]: #
[edikt:directives:end]: #
